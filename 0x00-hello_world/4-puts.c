#include <stdio.h>
/**
 * main -Entry point
 *
 * Return: Always 0 (Code for Success)
*/
int main (void)
{
	char str[55] = "\"Programming is like building a multilingual puzzle";
	puts(str);
	return (0);
}
